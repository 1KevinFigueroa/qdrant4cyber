services:
  chromadb:
    image: chromadb/chroma:latest
    container_name: chromadb
    ports:
      - "9000:8000"
    volumes:
      - chroma_data:/chroma/chroma
    restart: unless-stopped

volumes:
  chroma_data:
    name: chroma_data	